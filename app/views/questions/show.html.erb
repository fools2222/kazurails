
<div class="row">
    <div class="col-md-12">
        <h2><%= @question.title %></h2>
        <div>
            content:<%= @question.content %>
        </div>
        <div>
            name:<%= @question.name %>
        </div>
    </div>
</div>
    <hr>
    <div>
    <h3>answer</h3>
    <table class="table table-striped">
        <%if @question.answers.any? %>
        <thead class="thead thead-light">
            <tr>
                <td>answer</td>
                <td>name</td>
                <td>menu</td>
            </tr>
        </thead>
        <tbody>
            <%@question.answers.each do |p| %>
            <tr>
                <td><%= p.content %></td>
                <td><%= p.name %></td>
                <td>[<%= link_to "edit",edit_question_answer_path(@question,p) %>][<%= link_to "delete",question_answer_path(@question,p),method: :delete %>]</td>
            </tr>
            <%end %>
        </tbody>
        <%else %>
        <p>回答なし</p>

        <%end %>
    </table>
    
    </div>

    <h3>new answer</h3>
    <%= form_with model:[ @question,@answer],local: true do |p| %>
    <%= p.hidden_field :question_id,{value:@question.id} %>
        <div class="form-group">
            <label>name</label>
            <%= p.text_field :name,class:"form-control" %>
        </div>
        <div class="form-group"> 
        <label>content</label>
        <%= p.text_area :content,class:"form-control" %>
        </div>
        <div class="text-center">
            <%= p.submit "回答",class:"btn btn-primary" %>
        </div>
    <%end %>
    <div class="btn btn-primary">
        <%= link_to "戻る",root_path %>
    </div>

