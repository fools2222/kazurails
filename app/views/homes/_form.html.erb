

<%= form_with @inquiry do |f| %>
  <div class="form-group">
    <%= f.label :name, 'お名前' %>
    <%= f.text_field :name, class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :message, 'お問い合わせ内容' %>
    <%= f.text_area :message, class: 'form-control' %>
  </div>
    
  <%= f.submit '送信', class: 'btn btn-primary' %>
<% end %>